import { Rect } from './types';
export declare const textToCanvas: (lines: string[], fontStyle: string, color: string, lineHeightScale?: number) => HTMLCanvasElement;
export declare const imageBounds: (imageData: ImageData) => Rect;
